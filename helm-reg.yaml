
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: latest

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "lms-iuonly-service-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "reg"

serviceAccountName: lms-iuonly-impl-reg

cluster: es-test