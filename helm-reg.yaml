
projectedSecret:
  - secret:
      name: "properties-2021-03-09"

image:
  tag: latest

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "lms-iuonly-service-reg.apps-test.iu.edu"
      paths: []